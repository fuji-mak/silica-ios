{
  "name": "silica-api",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "db:local": "wrangler d1 migrations apply silica-notion-auth-db --local",
    "db:remote": "wrangler d1 migrations apply silica-notion-auth-db --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.108.0"
  }
}
